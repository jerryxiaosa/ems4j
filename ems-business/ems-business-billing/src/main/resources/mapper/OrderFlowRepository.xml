<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="info.zhihui.ems.business.billing.repository.OrderFlowRepository">

    <update id="updateBalanceSnapshotByConsumeId">
        update energy_account_order_flow
        set begin_balance = #{beginBalance},
            end_balance = #{endBalance}
        where consume_id = #{consumeId}
        limit 1
    </update>

</mapper>
