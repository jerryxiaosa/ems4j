<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="info.zhihui.ems.foundation.user.repository.UserThirdPartyBindRepository">

    <select id="selectByUserPlatformAndAppId" resultType="info.zhihui.ems.foundation.user.entity.UserThirdPartyBindEntity">
        select *
        from sys_user_third_party_bind
        where is_deleted = 0
          and platform = #{platform}
          and user_id = #{userId}
          and app_id = #{appId}
        order by id desc
        limit 1
    </select>

    <update id="updateByUserPlatform">
        update sys_user_third_party_bind
        set third_party_user_id = #{entity.thirdPartyUserId},
            third_party_union_id = #{entity.thirdPartyUnionId},
            phone = #{entity.phone},
            last_login_time = #{entity.lastLoginTime},
            update_time = #{entity.updateTime},
            update_user = #{entity.updateUser},
            update_user_name = #{entity.updateUserName}
        where is_deleted = 0
          and user_id = #{entity.userId}
          and platform = #{entity.platform}
          and app_id = #{entity.appId}
    </update>
</mapper>
